class GameConstants {
  static double blockSize = 50.0;
  static const double gravity = 400.0; // Pixels per second squared
  static const double playerSpeed = 200.0; // Pixels per second
  static const int columns = 7; // Number of columns in the grid
}
