name: running_tracker
description: "🏃‍♂️ Professional running route tracking app - Record every step of your journey!"
publish_to: "none"

# Google Play版本配置
# versionName (1.0.5) + versionCode (6)
version: 1.0.5+6

environment:
  sdk: ^3.5.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # UI组件
  cupertino_icons: ^1.0.8

  # 状态管理
  provider: ^6.1.2

  # 位置和地图
  geolocator: ^10.1.0
  location: ^5.0.3
  # 添加Google Maps作为备用方案
  google_maps_flutter: ^2.12.3

  # 数据存储
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  shared_preferences: ^2.2.3

  # 权限管理
  permission_handler: ^11.0.1

  # 网络请求
  http: ^1.1.0

  # 图片选择和处理
  image_picker: ^1.0.7
  gal: ^2.3.0
  image: ^4.1.7

  # 实用工具
  intl: ^0.20.2
  package_info_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of this
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # 自动热重载工具
  hotreloader: ^4.3.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # 启用国际化代码生成
  generate: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-variant files and is
  # referenced in Dart code using either of the following syntaxes:
  # AssetImage('assets/logo.png'), or AssetImage('assets/logo.png', package: 'my_package')
  #
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this format:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  # 添加图标资源
  assets:
    - assets/icons/
    - assets/images/
